components:
  - type: function
    name: test_function
    files:
      - ["src/test_function/*", "."]
  - type: container
    name: test_svc
    entry: "main"
    isNative: true
    files:
      - ["src/test_svc/*", "."]

config:
  prod:
    env:ENV_NAME: prod
  local:
    env:ENV_NAME: local
